import { CloseWindowCatcherDirective } from './close-window-catcher.directive';

describe('CloseWindowCatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseWindowCatcherDirective();
    expect(directive).toBeTruthy();
  });
});
